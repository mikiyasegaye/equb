import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#23224F",
    },
    secondary: {
      main: "#445765",
    },
    error: {
      main: "#f00",
    },
    background: {
      default: "#fff",
    },
  },
});
